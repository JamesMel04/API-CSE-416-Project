import { LeagueSettings } from "../../types";

export const mockLeagueSettings: LeagueSettings = {
    budget: 260,      // dollars per team
    rosterSize: 23,   // total players per fantasy team
    teamCount: 12,    // number of fantasy teams in the league
};